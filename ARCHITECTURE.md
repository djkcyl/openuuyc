# OpenUUYC 代码结构

OpenUUYC 当前仍只构建 Windows 产品。这里定义现有代码的职责、资源所有权和后续平台接入位置；并不表示已经实现其他系统。

## 目录与职责

| 目录 | 职责 |
| --- | --- |
| `src/account` | HTTP 账号接口、登录和凭据、设备资料、协助记录、设备能力策略及账号配置存储 |
| `src/session` | 设备会话复用、在线状态、连接建立、重连、观看附着和最终释放；组装控制端握手请求 |
| `src/features` | 串流设置、本机被控、输入、剪贴板、文件传输、端口转发等业务的状态、权限、协议处理与持久化 |
| `src/transport` | Socket.IO 信令、WebRTC、KCP、RTP 接收与修复；负责实际承载、收发任务和反馈 |
| `src/protocol` | 公共能力数据、协商矩阵和已核实的官方版本常量；不执行登录、界面或系统操作 |
| `src/media` | 媒体选项、格式、颜色、几何、音频、解码接口与管理、编码参数和软件编码适配 |
| `src/platform` | 编译时选择真实平台实现；Windows 采集、GPU 编码、DXVA、共享纹理、显示探测及 D3D11/DXGI 图形实现 |
| `src/application` | 设备中心、观看窗口、界面事件、观看解码流水线的编排、快捷键及壁纸展示 |
| `src/ui` | 共用窗口循环和界面控件；主题仍集中在 `theme.rs`，控件仍集中在 `controls.rs` |
| `src/diagnostics` | 日志、性能数据及 RTP 捕获；日志设置的模块 ID 与保存格式保持稳定 |
| `src/plugins` | 插件协议、运行隔离、节点图及编辑器；不与普通业务窗口的进程模型混合 |
| `crates/h264-core` | 独立、无 Windows 依赖的 Rust H.264 编解码核心；独立构建及原有 CPU 指令集测试 |
| `vendor` | 已使用的第三方实现与驱动绑定，保留其许可证和原有测试 |

`main.rs` 只负责参数解析和入口选择，GUI/CLI 仍生成同一个 OpenUUYC。不要通过可执行文件名选择角色或凭据命名空间。

## 主要模块的边界

`transport/rtc` 的 `peer` 负责主控角色编排；`negotiation` 负责 SDP、编解码和扩展注册；`channels` 负责业务通道绑定及有序发送；`tracks` 负责订阅与帧完成反馈；`receive`、`ingress`、`feedback`、`clock` 分别负责收包交付、排序修复、NACK/关键帧与时钟映射。`statistics` 观察实际路线并生成上报，`workers` 管理会话任务的取消和等待。

`transport/rtc/core.rs::ConnectionCore`是主控和被控共同持有的连接核心，拥有同一PeerConnection和KCP实例，集中执行共同传输设置、offer/answer的创建与安装、协商后启用CONTROL、提交ICE配置以及底层关闭。构造失败/异常Drop也由核心安排实际传输释放；角色拥有者先取消自己的业务，再等待底层关闭及各自资源回收。核心不持有账号、屏幕、输入、文件或UI对象，KCP仅通过字节接收回调连接业务模块。回调和媒体任务使用连接引用，不获得第二份独立核心。

`transport/official_receiver` 将载荷解析、包组装、帧依赖和离线回放分开。`transport/signal/wire.rs` 负责 Socket.IO/Engine.IO 帧与有界 SDP 解压；`negotiation.rs` 负责信令请求和会话描述。信令会话继续拥有 ACK、重连和网络切换时序。

`features/stream_control` 保留一个设备会话的共享状态，`wire` 定义 protobuf 字段与封装，`handshake` 处理通道和握手，`settings` 处理设置提案与确认，`tracks` 处理采集激活和轨道登记，`input` 处理输入与光标策略。显示设置、拓扑、批注和麦克风仍是各自的业务模块。拆文件不创建多个互相竞争的状态副本。

`features/host` 是本机被控业务。`settings` 按账号及本机设备保存允许配置；`access` 分开管理配置、在线接入代次和单次连接租约；`desktop` 负责能力准备及连接所请求屏幕的媒体配置。设置页只提交配置与断开操作，不能持有采集源或媒体线程。`transport/signal/host` 随账号房间同步许可并接入被控连接，`peer/capture_worker` 编排采集和编码，驱动对象继续由原线程持有。原生后端只接收必要参数及授权状态检查回调，不依赖账号或被控会话对象。源描述、编码输出和码率参数位于 `media`，驱动资源位于 `platform/windows`。

`features/host/displays`持有本次连接的原始布局、按控制端保存的显示偏好、虚拟目标GUID与恢复记录；`peer/screens`管理固定视频轨道池及逐屏采集，不把Windows目标编号当作协议ID。`platform/windows/display`提供库存、模式/DPI、公开IDD IOCTL、安装和恢复守护原语。虚拟驱动是用户明确安装的可选组件，已有第三方设备不归本程序管理。同一二进制的隐藏display-recovery角色只在显示变更期间守护退出恢复，不承载普通业务窗口；display-driver-install/display-driver-uninstall是明确本机操作触发的提权驱动管理角色；卸载核对硬件节点、原始INF和占用，不强制删除被引用的驱动包。

控制端与被控端共用`transport/uu_kcp`及底层ICE/DTLS/SCTP实现；普通CONTROL发送统一经`UuKcpControl::send_control`检查通道状态并选择已协商承载，失败不跨承载重发。SDP扩展生成/版本解析、线上protobuf定义和双端能力矩阵也共用，不为角色另外复制协议常量或选择算法。输入消息额外的代次检查、截止时间和按键释放属于控制端业务策略，仍通过同一KCP内核发送。

角色编排保持独立：控制端产生offer和业务请求、接收解码呈现；被控端按offer应答、验证本地被控许可、采集编码并产生状态报告。共同协议变更须核对两端入口和消费者；不能为了目录统一合并入站副作用权限、收发拥塞控制或不同拥有者的取消状态。

两端切路命令中的数值含义、候选早到处理和媒体注册仍按官方角色入口决定，再交给共同核心执行。不得用同一个role判断树将这些不同的协议规则混成默认行为；共同核心也不增加帧队列、线程、重试或动态后端空壳。

`application/app/runtime` 拥有设备中心后台任务，`messages` 定义与界面之间的命令和事件。`session/controller/connection` 解析目标和恢复偏好，`viewing` 管理窗口附着与重连。界面响应与网络任务的取消流程没有因拆分而合并。

GUI启动加载包含本机媒体能力准备阶段，任务由runtime持有和取消，账号所属host持有已验证的数值缓存；启动与入站连接使用同一准备入口。关闭窗口须等待原生准备取消，不把任务抛给无人拥有的后台线程；缓存不能持有采集或编码器资源，也不代表被控许可。

`application/viewer/decode_pipeline` 管理解码代次、换流和帧交付，`hud` 只负责性能面板。Windows 播放器内，`connection` 处理连接窗口与事件循环，`player` 处理播放窗口及本地输入，`chrome` 处理标题栏和窗口几何，`worker` 持有渲染线程，`renderer` 持有呈现资源和插件处理。底层着色器、交换链及共用界面图形实现位于 `platform/windows`。

## 所有权与行为契约

| 所有者 | 必须保留的契约 |
| --- | --- |
| 账号生命周期 | 凭据命名空间不变；退出清理账号任务；普通窗口关闭不执行退出账号 |
| 设备会话 | 按真实设备 ID 复用；观看、文件和转发各自持有使用权；关闭一个窗口不误关其他业务 |
| RTC 会话 | 工作任务随所属连接取消并等待；可靠通道发送不在 SCTP 分片入队中途取消 |
| 设置状态 | 完整快照、请求序号、消息来源、远端确认和实际屏幕报告保留各自含义 |
| 观看窗口/解码代次 | 切屏、换流、失焦和关闭保留输入释放、旧帧淘汰及解码完成反馈 |
| 软件解码槽位 | 原有跨窗口、跨进程独占范围不变；不能通过增加后端实例绕开 |
| 本机被控 | 持久化允许配置与连接分离；撤销配置立即使旧许可失效；断开只结束当前会话；媒体租约仍绑定本次源/代次，构造失败、关闭或替换后旧回调不能生效 |
| 原生资源 | COM、驱动对象、GPU 同步、纹理租约及线程析构顺序由各自所有者负责 |

接口调整优先传入参数、已有通知或必要回调，不让底层模块持有整个界面或账号对象。不要为结构统一增加队列、线程、动态分派、缓存或延迟。只有确实需要多个实现且明确对象安全和资源契约时，才引入新的 trait。

## 增加平台或业务

新增业务优先放入独立 `features` 模块，复用现有账号和设备会话，以明确的句柄/命令接入窗口。业务私有消息与消费者一起维护；只有多个业务确实共享的值和契约才上移，避免将 `protocol` 变成新的大杂烩。

新增平台时，在 `platform` 的选择入口接入实际后端，复用协议、协商和独立编解码核心。先明确原生帧/纹理所有权、取消、输出通知、能力探测及零拷贝约束，再补对应实现。跨系统 GPU 资源不能假装成 Windows 句柄。现有 `media/decode_api`、捕获源描述及编码参数是复用起点，不是对所有未来 API 的保证。

当前 Windows 的窗口/输入壳层、OLE 剪贴板、凭据服务、软件解码槽位和插件宿主仍有真实系统依赖，分别留在明确命名的功能适配模块内。未来支持其他系统时，这些入口也需要实际实现和验证；不能只增加 `cfg` 或空后端就宣布跨平台。当前主程序的 Windows 构建限制保留。

新增代码应直接引用所属模块的依赖，避免继续通过父模块批量导入外部库。已有功能内的私有实现只向所属模块开放；对外入口集中在模块根。构建路径、嵌入资源、日志分类和许可证引用必须随文件迁移一起核对。

## 检查入口

```powershell
cargo fmt --all -- --check
cargo check --all-targets
cargo test --manifest-path crates/h264-core/Cargo.toml --lib
cargo build --release
```

结构检查不能代替产品验证。完成一轮集中改动后，验证登录态恢复、设备列表、实际软/硬件播放与关闭、本机被控配置恢复及连接建立/结束，并按受影响功能覆盖业务窗口和资源释放。真实电源、设备删除、物理显示变更等操作继续遵守原有用户授权范围。

本轮及后续任务的完成情况和验证边界只在本地 `docs/status.md` 维护；本文件描述结构与维护规则，不维护待办清单。
